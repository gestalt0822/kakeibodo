<%= form_for(@category) do |f| %>
  <%= f.label :項目名 %>
  <%= f.text_field :name, autofocus: true , class: "form-control" %>
  <%= f.submit '追加', class: 'btn btn-default btn-xs btn-info' %>
<% end %>
